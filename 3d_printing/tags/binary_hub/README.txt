Files list:
  old resource file: binary_hub_old.blend
  up to date resource: binary_hub_new.blend
  stl parts:
    base.stl
    binary1.stl
    binary2.stl
    hub1.stl
    hub2.stl
  assembly project: binary_hub.3mf

Assembly steps (if provided assembly is not used):
1. Open new project and set up printer model, plate type and nozzle diameter. 
2. Add all parts as a 'single object with multiple parts' (select all part files and dragNdrop them together).
3. Set up Filaments and assign different filaments to different types of parts (RMB -> edit parameter table).

